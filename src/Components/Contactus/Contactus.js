import React from 'react';
import {Container} from "react-bootstrap";

const Contactus = () => {
    return (
        <div>
            <Container></Container>
        </div>
    );
};

export default Contactus;